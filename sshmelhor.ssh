# O primeiro comando vc só roda uma vez, dentro de /worspace/nome-do-repo
# ssh-keygen -t rsa -N '' -f $(pwd)/id_rsa <<< y;
mkdir -p ~/.ssh/
cp $(pwd)/id_rsa $(pwd)/id_rsa.pub ~/.ssh/
echo "Chave ssh foi copiada para o diretório ~/.ssh/"

